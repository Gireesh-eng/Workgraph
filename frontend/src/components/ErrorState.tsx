export default function ErrorState({ message, onRetry }: { message: string; onRetry?: () => void }) { return <div className="card mt-6 flex min-h-[320px] flex-col items-center justify-center px-5 py-16 text-center motion-safe:animate-fade-up"><div className="flex h-14 w-14 items-center justify-center rounded-full bg-surface-sunken"><svg className="h-7 w-7 text-ink-900" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.5"><path d="M12 8v5m0 3h.01M5 20h14L12 4 5 20Z" /></svg></div><h2 className="mt-5 text-[19px] font-semibold">Something went wrong</h2><p className="mt-2 max-w-md text-[15px] leading-6 text-ink-600">{message}</p>{onRetry && <button onClick={onRetry} className="mt-6 h-11 rounded-control bg-ink-900 px-5 text-[14px] font-semibold text-paper transition-transform duration-120 hover:scale-[1.02] active:scale-[.98]">Try again</button>}</div>; }
